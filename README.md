# business-card

Prosta strona wizytówkowa (HTML/CSS/JS, bez build stepu).

## Podgląd lokalnie

Otwórz `index.html` w przeglądarce, albo uruchom lokalny serwer, np.:

```bash
npx serve .
```

## Hosting

Repo jest gotowe pod GitHub Pages: Settings → Pages → Deploy from branch → `main` / `root`.
